# Build output
target/
dist/

# Node
node_modules/

# Deployment secrets — never commit these
deploy-config.json
deploy-config.mainnet.json
deploy-config.testnet.json
deploy-config.dev.json
.env

# Compiled contract artifacts
*.wasm

# Logs
*.log

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.claude/

# Soroban test snapshots (auto-generated by cargo test, not for version control)
test_snapshots/
**/test_snapshots/

# Benchmark results (auto-generated, committed only as baseline when intentional)
bench_results.txt

# Lock files for sub-packages (keep root Cargo.lock, ignore sdk sub-lock)
sdk/package-lock.json
